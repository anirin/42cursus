t_getchar get_newline(fd, save)
{
    t_getchar tmp;

    if ()
    tmp.bufp = tmp.buf;
    tmp.check = read(fd, bufp, BUFFER_SIZE); 


}

char *get_next_line(int fd);
{
    static t_getchar save;

    save.bufp = save.buf;
    save.check = read(fd, bufp, BUFFER_SIZE);
}